import styled from "styled-components";

export const WhatsappContactStyled = styled.a`
  position: fixed;
  bottom: 2rem;
  right: 2rem;

  @media screen and (min-width: 1024px){
    bottom: 4rem;
    right: 4rem;
  }
`