import styled from "styled-components";

export const AchievementStyled = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
`

export const AchievementValue = styled.span`
  font-family: var(--montserrat);
  font-size: 6.4rem;
  font-weight: 700;
  color: var(--color-primary);
`

export const AchievementText = styled.p`
  font-family: var(--nunita);
  font-size: 2.4rem;
  font-weight: 400;
  color: var(--color-text-primary);
`
